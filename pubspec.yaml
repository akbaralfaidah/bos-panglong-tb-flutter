name: bos_panglong_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # File & Media
  file_picker: ^8.0.0
  image_picker: ^1.1.2 
  csv: ^6.0.0 
  blue_thermal_printer: ^1.2.3

  # Izin & Utilitas Sistem
  permission_handler: ^11.3.1 
  path_provider: ^2.1.3 
  share_plus: ^9.0.0 

  # Database
  sqflite: ^2.3.3 
  path: ^1.9.0

  # UI, Format & Analitik
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  intl: ^0.19.0
  local_auth: ^3.0.1
  
  # === TAMBAHAN BARU UNTUK LAPORAN SUPER ===
  fl_chart: ^0.68.0      # Untuk Grafik
  url_launcher: ^6.3.0   # Untuk Telepon / WhatsApp Pelanggan

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true